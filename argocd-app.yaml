apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudtestingapp
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/yourusername/CloudTestingApp.git # Change to your actual repo
    targetRevision: HEAD
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
